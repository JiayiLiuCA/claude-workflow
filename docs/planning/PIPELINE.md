# {项目名} Pipeline

本文档是 planning 体系的**薄核心**：原始需求、核心概念、step 拆分、schema 总览、契约索引、决议台账。各子系统的行为详细参考按域拆分在 `pipeline/` 目录（见 §7 索引），不放本文档——这是防止本文档随 step 数线性膨胀的关键约定。

<!-- bootstrap 阶段填充 §1-§3 与 §6；§4 / §5 / §7 由各 step 的 Close 阶段增量维护 -->

## 1. 原始需求

<!-- bootstrap：用户需求原文或忠实摘录。本节此后不改；需求变化走决议台账 + step 拆分调整 -->

## 2. 核心概念

<!-- bootstrap：领域名词定义，每个概念一小节。全项目（代码命名、文档、UI 用词）以此为准 -->

## 3. Implementation Step 拆分

<!-- bootstrap 生成草案（标 DRAFT 待用户 review）。每 step：目标一句话 + 范围要点 + 依赖关系。
拆分原则：每 step 完成后有用户可观察的行为增量；外部依赖集成的 step 单独拆。
完成后由 Close 阶段在末尾追加「✅ 完成于 {日期}」。
调整实施顺序时在本节顶部记录决议（日期 + 理由），step 编号不变，仅调实施时间。 -->

### Step 0: Bootstrap 可运行骨架

<!-- bootstrap：填充 Step 0 与后续所有 step 的草案 -->

## 4. Schema 总览

<!-- 真相源是 migration 与 model 代码；本节是当前状态的快照，由 Close 阶段同步。
每表一个小节：### 表名（Step N 落地，Step M 修改）+ 字段表格（名称 / 类型 / 约束 / 说明）。 -->

_尚无表。_

## 5. 已实现契约索引

**只留索引，一行一条；签名与 schema 细节以实际代码为准，不在此复制。** 由 Close 阶段追加。

### API

| 契约 | 用途 | 实现位置 | Step |
|---|---|---|---|

### 前端组件

| 组件 | 用途 | 实现位置 | Step |
|---|---|---|---|

### 模块 / 服务

| 模块 | 用途 | 实现位置 | Step |
|---|---|---|---|

## 6. 决议台账

所有跨 step 的待定项与已拍板决议，格式：

- `- [ ] 待决问题（预计 Step N 确定）`
- `- [x] 结论 + 理由（Step N 决议）`
- 推翻旧决议：`- [x] ~~原结论~~ → 新结论 + 推翻原因（Step M 推翻）`——保留历史，不删除

<!-- bootstrap：登记所有待定技术选型与开放问题 -->

## 7. 域详细参考索引

各子系统的行为详细参考在 `pipeline/<domain>.md`，由 Close 阶段创建与维护。收录标准见 `pipeline/README.md`（一句话版：**代码读不出来的才写**）。

| 域 | 文件 | 内容摘要 |
|---|---|---|
